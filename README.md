# sap-to-gcp-pipeline
End-to-end SAP to GCP data migration pipeline in Python. Extracts SAP table data, validates and cleanses it, maps schema, and loads into BigQuery via Cloud Storage staging with zero data-loss verification through row-count reconciliation.
